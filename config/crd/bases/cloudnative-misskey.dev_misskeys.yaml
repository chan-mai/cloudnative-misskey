---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: misskeys.cloudnative-misskey.dev
spec:
  group: cloudnative-misskey.dev
  names:
    kind: Misskey
    listKind: MisskeyList
    plural: misskeys
    shortNames:
    - mk
    singular: misskey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .spec.search.provider
      name: Search
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Misskey is the Schema for the misskeys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MisskeySpec defines the desired state of a Misskey instance.
            properties:
              app:
                description: App configures the web/API server Deployment (runs with
                  MK_ONLY_SERVER).
                properties:
                  autoscaling:
                    description: |-
                      Autoscaling replaces the static Replicas with a HorizontalPodAutoscaler
                      (CPU/memory) or, when queues are set, a KEDA ScaledObject (BullMQ queue
                      depth). HPA needs metrics-server; queue scaling needs KEDA installed.
                    properties:
                      enabled:
                        default: true
                        description: Enabled toggles autoscaling when the block is
                          present.
                        type: boolean
                      maxReplicas:
                        description: MaxReplicas is the upper bound.
                        format: int32
                        minimum: 1
                        type: integer
                      minReplicas:
                        description: |-
                          MinReplicas is the lower bound. Default 1; use >=2 so the PodDisruptionBudget
                          (maxUnavailable=1) still allows node drains.
                        format: int32
                        minimum: 0
                        type: integer
                      queues:
                        description: |-
                          Queues, when set, switches the mechanism to a KEDA ScaledObject that scales on
                          BullMQ queue wait-list depth. Requires KEDA in the cluster. Meaningful for the
                          worker; typically deliver and inbox.
                        items:
                          description: QueueScaleTrigger scales on the wait-list depth
                            of one BullMQ queue.
                          properties:
                            listLength:
                              description: ListLength is the target average wait-list
                                length per replica.
                              format: int32
                              minimum: 1
                              type: integer
                            listName:
                              description: |-
                                ListName overrides the exact Redis key watched. Defaults to the computed
                                BullMQ wait-list key for Name.
                              type: string
                            name:
                              description: Name is the BullMQ queue name, e.g. deliver,
                                inbox.
                              type: string
                          required:
                          - listLength
                          - name
                          type: object
                        type: array
                      targetCPUUtilizationPercentage:
                        description: TargetCPUUtilizationPercentage adds a CPU utilization
                          metric. Unset omits it.
                        format: int32
                        type: integer
                      targetMemoryUtilizationPercentage:
                        description: TargetMemoryUtilizationPercentage adds a memory
                          utilization metric. Unset omits it.
                        format: int32
                        type: integer
                    required:
                    - maxReplicas
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector constrains scheduling to matching nodes.
                    type: object
                  replicas:
                    description: Replicas is the desired number of pods.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources describes compute requests/limits for the
                      container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations allow scheduling onto tainted nodes.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy controls what happens to data-bearing resources (the CNPG
                  cluster, Redis/MeiliSearch StatefulSets and generated key Secrets) when this
                  Misskey is deleted. Delete (default) garbage-collects everything via owner
                  references. Retain orphans them so the data survives; recreating the Misskey
                  with the same name re-adopts them.
                enum:
                - Delete
                - Retain
                type: string
              egressIsolation:
                description: EgressIsolation configures egress isolation (opt-in).
                  See EgressIsolationSpec.
                properties:
                  allowedNamespaces:
                    description: |-
                      AllowedNamespaces are additional egress-permitted namespace names on any
                      port, e.g. an in-cluster SMTP relay or object store.
                    items:
                      type: string
                    type: array
                  dnsNamespace:
                    default: kube-system
                    description: |-
                      DNSNamespace runs the cluster DNS (CoreDNS), allowed on :53. Defaults to
                      kube-system.
                    type: string
                  enabled:
                    description: Enabled creates the egress NetworkPolicies. Default
                      false (opt-in).
                    type: boolean
                type: object
              extraConfig:
                description: |-
                  ExtraConfig is raw YAML appended verbatim to the generated default.yml.
                  Use it for settings not modeled by this CRD.
                type: string
              idGenerationMethod:
                default: aidx
                description: |-
                  IDGenerationMethod is Misskey's note/user id format. Immutable after the
                  instance has been initialized; when migrating an existing database, set it
                  to the value the database was created with.
                enum:
                - aid
                - aidx
                - meid
                - ulid
                - objectid
                type: string
              image:
                description: Image is the Misskey server container image. The app
                  and worker share it.
                type: string
              imagePullSecrets:
                description: ImagePullSecrets used to pull the Misskey image.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              ingress:
                description: |-
                  Ingress configures the Ingress exposing the proxy (or the app when the
                  proxy is disabled).
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations added to the Ingress object.
                    type: object
                  className:
                    default: nginx
                    description: ClassName sets ingressClassName, e.g. nginx or traefik.
                    type: string
                  enabled:
                    default: true
                    description: Enabled toggles Ingress creation.
                    type: boolean
                  host:
                    description: Host overrides the ingress host. Defaults to the
                      host part of spec.url.
                    type: string
                  tlsSecretName:
                    description: TLSSecretName enables a TLS block referencing the
                      named secret. Optional.
                    type: string
                type: object
              migration:
                description: Migration configures the schema-migration Job behavior.
                properties:
                  createIndexConcurrently:
                    description: |-
                      CreateIndexConcurrently sets MISSKEY_MIGRATION_CREATE_INDEX_CONCURRENTLY=1 on the
                      migration Job so index-creating migrations use CREATE INDEX CONCURRENTLY (which
                      does not block writes) instead of a locking CREATE INDEX, keeping deploys
                      non-blocking on large tables such as note. Default false (opt-in), matching
                      Misskey's upstream default of running all migrations in a single atomic
                      transaction. Tradeoffs when true: per-migration transactions (not one atomic
                      batch), slower index builds, and a failed concurrent build can leave an invalid
                      index needing manual cleanup.
                    type: boolean
                type: object
              monitoring:
                description: |-
                  Monitoring generates Prometheus ServiceMonitor/PodMonitor for the instance's
                  managed backends (PostgreSQL/Redis/MeiliSearch).
                properties:
                  enabled:
                    description: |-
                      Enabled generates ServiceMonitor/PodMonitor and turns on each backend's
                      metrics endpoint (CNPG :9187, a redis_exporter sidecar, MeiliSearch metrics).
                    type: boolean
                  interval:
                    default: 30s
                    description: Interval is the scrape interval. Default 30s.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels are added to the generated ServiceMonitor/PodMonitor so your Prometheus
                      serviceMonitorSelector/podMonitorSelector matches them (e.g. release: kps).
                    type: object
                  redisExporterImage:
                    default: oliver006/redis_exporter:v1.62.0-alpine
                    description: RedisExporterImage is the redis_exporter sidecar
                      image for standalone Redis.
                    type: string
                type: object
              networkIsolation:
                description: |-
                  NetworkIsolation configures ingress isolation for this instance's backend
                  pods (app/worker/redis/meilisearch).
                properties:
                  allowedNamespaces:
                    description: |-
                      AllowedNamespaces are namespace names (matched by the
                      kubernetes.io/metadata.name label) permitted to reach the isolated backend
                      pods in addition to intra-instance traffic, e.g. a monitoring namespace for
                      Prometheus scraping.
                    items:
                      type: string
                    type: array
                  enabled:
                    default: true
                    description: Enabled creates the isolation NetworkPolicy. Default
                      true.
                    type: boolean
                type: object
              postgres:
                description: Postgres configures the PostgreSQL backend (CNPG-managed
                  or external).
                properties:
                  backup:
                    description: Backup enables barmanObjectStore backups on the CNPG
                      cluster.
                    properties:
                      destinationPath:
                        description: DestinationPath is the object store path, e.g.
                          s3://bucket/path.
                        type: string
                      endpointURL:
                        description: EndpointURL of the S3-compatible object store.
                        type: string
                      retentionPolicy:
                        default: 30d
                        description: RetentionPolicy for backups, e.g. "30d".
                        type: string
                      s3Credentials:
                        description: S3Credentials references the access/secret keys.
                        properties:
                          accessKeyId:
                            description: AccessKeyID references the access key id.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          secretAccessKey:
                            description: SecretAccessKey references the secret access
                              key.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        required:
                        - accessKeyId
                        - secretAccessKey
                        type: object
                      schedule:
                        description: Schedule is a 6-field cron for a CNPG ScheduledBackup.
                          Empty disables it.
                        type: string
                    required:
                    - destinationPath
                    type: object
                  database:
                    default: misskey
                    description: Database is the DB name created by CNPG initdb.
                    type: string
                  external:
                    description: External points Misskey at an existing PostgreSQL.
                      When set, CNPG is not used.
                    properties:
                      database:
                        description: Database name.
                        type: string
                      host:
                        description: Host of the external PostgreSQL.
                        type: string
                      passwordSecret:
                        description: PasswordSecret references the DB password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 5432
                        description: Port of the external PostgreSQL.
                        format: int32
                        type: integer
                      user:
                        description: User name.
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecret
                    - user
                    type: object
                  imageName:
                    default: ghcr.io/cloudnative-pg/postgresql:17
                    description: ImageName overrides the CNPG PostgreSQL image.
                    type: string
                  instances:
                    default: 1
                    description: Instances is the CNPG cluster size (1 = single, >=2
                      = HA with replicas).
                    format: int32
                    minimum: 1
                    type: integer
                  owner:
                    default: misskey
                    description: Owner is the DB owner role created by CNPG initdb.
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Parameters merged into postgresql.parameters of the
                      CNPG cluster.
                    type: object
                  pooler:
                    description: |-
                      Pooler provisions CNPG PgBouncer connection poolers in front of the cluster,
                      multiplexing app/worker connections so max_connections is not exhausted as
                      pods scale out.
                    properties:
                      enabled:
                        default: true
                        description: Enabled toggles the poolers when the block is
                          present.
                        type: boolean
                      instances:
                        default: 2
                        description: Instances is the PgBouncer pod count per pooler.
                        format: int32
                        minimum: 1
                        type: integer
                      parameters:
                        additionalProperties:
                          type: string
                        description: |-
                          Parameters merged into pgbouncer.parameters, e.g. max_client_conn,
                          default_pool_size.
                        type: object
                      poolMode:
                        default: transaction
                        description: 'PoolMode is the PgBouncer pool mode: transaction,
                          session or statement.'
                        enum:
                        - transaction
                        - session
                        - statement
                        type: string
                      resources:
                        description: Resources for each PgBouncer pod.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  readOffload:
                    description: |-
                      ReadOffload wires Misskey dbReplications onto CNPG standby replicas so reads
                      are load-balanced off the primary. Defaults on when instances >= 2; set false
                      to opt out.
                    type: boolean
                  resources:
                    description: Resources for each CNPG instance.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 20Gi
                    description: Storage size of each CNPG instance PVC.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: StorageClassName for the CNPG PVCs.
                    type: string
                type: object
              proxy:
                description: Proxy configures the Caddy reverse proxy fronting the
                  app.
                properties:
                  clientIPHeader:
                    description: |-
                      ClientIPHeader, when set, overrides X-Real-IP and X-Forwarded-For from the
                      named header (e.g. CF-Connecting-IP behind Cloudflare). When empty, the
                      upstream's X-Forwarded-For is preserved, trusting private-range upstreams
                      via Caddy trusted_proxies.
                    type: string
                  enabled:
                    default: true
                    description: Enabled toggles the Caddy proxy. When false the Ingress
                      targets the app directly.
                    type: boolean
                  image:
                    default: caddy:2
                    description: Image for the Caddy proxy and maintenance containers.
                    type: string
                  maintenance:
                    description: Maintenance configures the fallback page served when
                      the app is down.
                    properties:
                      enabled:
                        default: true
                        description: Enabled toggles the maintenance fallback Deployment.
                        type: boolean
                      html:
                        description: HTML is the page body served during maintenance.
                          A default is used when empty.
                        type: string
                      reloadSeconds:
                        default: 30
                        description: |-
                          ReloadSeconds sets the auto-reload interval of the built-in maintenance
                          page, in seconds. 0 disables auto-reload. Ignored when a custom html is set.
                        format: int32
                        minimum: 0
                        type: integer
                      statusCode:
                        default: 200
                        description: |-
                          StatusCode is the HTTP status returned for the maintenance page. Default 200
                          so the page renders as a normal 2xx response. Set to 503 for the standard
                          "temporarily unavailable" semantics. Note that /api/* is always excluded
                          from the maintenance page and returns the real backend status so external
                          health checks are not masked.
                        format: int32
                        maximum: 599
                        minimum: 100
                        type: integer
                    type: object
                  replicas:
                    description: Replicas of the proxy Deployment.
                    format: int32
                    type: integer
                  resources:
                    description: Resources for the proxy container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              redis:
                description: Redis configures the Redis backend (managed StatefulSet
                  or external).
                properties:
                  appendOnly:
                    default: true
                    description: |-
                      AppendOnly enables Redis AOF (--appendonly yes) for job-queue durability
                      across restarts. Default true; set false for pure-cache use.
                    type: boolean
                  external:
                    description: External points Misskey at an existing Redis. When
                      set, no Redis is managed.
                    properties:
                      host:
                        description: |-
                          Host of the external Redis. With Sentinels set this is ignored by the
                          client but still required by Misskey's config schema.
                        type: string
                      masterName:
                        description: MasterName is the sentinel master group name
                          (required with Sentinels).
                        type: string
                      passwordSecret:
                        description: PasswordSecret optionally references the Redis
                          password.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      port:
                        default: 6379
                        description: Port of the external Redis.
                        format: int32
                        type: integer
                      sentinels:
                        description: |-
                          Sentinels, when set, connects via Redis Sentinel (ioredis passthrough)
                          instead of host/port.
                        items:
                          description: RedisHostPort is a sentinel endpoint.
                          properties:
                            host:
                              type: string
                            port:
                              default: 26379
                              format: int32
                              type: integer
                          required:
                          - host
                          type: object
                        type: array
                    required:
                    - host
                    type: object
                  ha:
                    description: |-
                      HA enables operator-managed Sentinel HA (CloudNativePG-style, via the
                      OT-CONTAINER-KIT redis-operator) for managed Redis instances instead of a
                      single-pod StatefulSet. Requires the redis-operator installed in the cluster.
                    properties:
                      enabled:
                        default: true
                        description: Enabled toggles HA when the block is present.
                        type: boolean
                      image:
                        default: quay.io/opstree/redis:v8.2.5
                        description: Image is the redis image driven by the operator's
                          RedisReplication.
                        type: string
                      replicas:
                        default: 3
                        description: |-
                          Replicas is the RedisReplication cluster size: total redis nodes
                          (1 primary + N-1 replicas). 3 gives one primary and two replicas.
                        format: int32
                        minimum: 2
                        type: integer
                      sentinelImage:
                        default: quay.io/opstree/redis-sentinel:v8.2.5
                        description: SentinelImage is the redis-sentinel image for
                          the operator's RedisSentinel.
                        type: string
                      sentinels:
                        default: 3
                        description: Sentinels is the RedisSentinel cluster size.
                          Use an odd number for quorum.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  image:
                    default: redis:8-alpine
                    description: |-
                      Image for the managed standalone Redis StatefulSet (HA off). Redis 8 is
                      AGPLv3-licensed, matching this project.
                    type: string
                  maxMemory:
                    default: 400mb
                    description: MaxMemory passes --maxmemory to redis-server.
                    type: string
                  maxMemoryPolicy:
                    default: noeviction
                    description: |-
                      MaxMemoryPolicy sets --maxmemory-policy. Default noeviction, because this
                      Redis also backs the job queue (BullMQ) and an eviction policy such as
                      allkeys-lru would silently drop queued/delayed jobs under memory pressure.
                    type: string
                  networkPolicy:
                    default: true
                    description: |-
                      NetworkPolicy toggles a NetworkPolicy that limits ingress to the managed
                      Redis to app and worker pods. Default true. Only effective on CNIs that
                      enforce NetworkPolicy.
                    type: boolean
                  resources:
                    description: Resources for the managed Redis container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  roles:
                    description: |-
                      Roles configures per-role Redis separation. Each role (jobQueue, pubsub,
                      timelines, reactions) can point at its own managed instance or an external
                      Redis; an unset role falls back to the shared redis (Misskey omits the
                      redisForXxx block, which makes Misskey reuse the main redis).
                    properties:
                      jobQueue:
                        description: |-
                          RedisRole separates one Misskey Redis role. Presence separates the role;
                          External points it at an existing Redis, otherwise a dedicated instance is
                          provisioned (inheriting the default redis settings unless overridden here).
                        properties:
                          external:
                            description: |-
                              External points this role at an existing Redis. Mutually exclusive with the
                              managed override fields.
                            properties:
                              host:
                                description: |-
                                  Host of the external Redis. With Sentinels set this is ignored by the
                                  client but still required by Misskey's config schema.
                                type: string
                              masterName:
                                description: MasterName is the sentinel master group
                                  name (required with Sentinels).
                                type: string
                              passwordSecret:
                                description: PasswordSecret optionally references
                                  the Redis password.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              port:
                                default: 6379
                                description: Port of the external Redis.
                                format: int32
                                type: integer
                              sentinels:
                                description: |-
                                  Sentinels, when set, connects via Redis Sentinel (ioredis passthrough)
                                  instead of host/port.
                                items:
                                  description: RedisHostPort is a sentinel endpoint.
                                  properties:
                                    host:
                                      type: string
                                    port:
                                      default: 26379
                                      format: int32
                                      type: integer
                                  required:
                                  - host
                                  type: object
                                type: array
                            required:
                            - host
                            type: object
                          ha:
                            description: HA override for this role's managed instance.
                              Nil inherits spec.redis.ha.
                            properties:
                              enabled:
                                default: true
                                description: Enabled toggles HA when the block is
                                  present.
                                type: boolean
                              image:
                                default: quay.io/opstree/redis:v8.2.5
                                description: Image is the redis image driven by the
                                  operator's RedisReplication.
                                type: string
                              replicas:
                                default: 3
                                description: |-
                                  Replicas is the RedisReplication cluster size: total redis nodes
                                  (1 primary + N-1 replicas). 3 gives one primary and two replicas.
                                format: int32
                                minimum: 2
                                type: integer
                              sentinelImage:
                                default: quay.io/opstree/redis-sentinel:v8.2.5
                                description: SentinelImage is the redis-sentinel image
                                  for the operator's RedisSentinel.
                                type: string
                              sentinels:
                                default: 3
                                description: Sentinels is the RedisSentinel cluster
                                  size. Use an odd number for quorum.
                                format: int32
                                minimum: 1
                                type: integer
                            type: object
                          maxMemory:
                            description: MaxMemory override for this role's managed
                              instance.
                            type: string
                          maxMemoryPolicy:
                            description: MaxMemoryPolicy override for this role's
                              managed instance.
                            type: string
                          resources:
                            description: Resources override for this role's managed
                              container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This is an alpha field and requires enabling the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Storage override for this role's managed
                              PVC.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: StorageClassName override for this role's
                              managed PVC.
                            type: string
                        type: object
                      pubsub:
                        description: |-
                          RedisRole separates one Misskey Redis role. Presence separates the role;
                          External points it at an existing Redis, otherwise a dedicated instance is
                          provisioned (inheriting the default redis settings unless overridden here).
                        properties:
                          external:
                            description: |-
                              External points this role at an existing Redis. Mutually exclusive with the
                              managed override fields.
                            properties:
                              host:
                                description: |-
                                  Host of the external Redis. With Sentinels set this is ignored by the
                                  client but still required by Misskey's config schema.
                                type: string
                              masterName:
                                description: MasterName is the sentinel master group
                                  name (required with Sentinels).
                                type: string
                              passwordSecret:
                                description: PasswordSecret optionally references
                                  the Redis password.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              port:
                                default: 6379
                                description: Port of the external Redis.
                                format: int32
                                type: integer
                              sentinels:
                                description: |-
                                  Sentinels, when set, connects via Redis Sentinel (ioredis passthrough)
                                  instead of host/port.
                                items:
                                  description: RedisHostPort is a sentinel endpoint.
                                  properties:
                                    host:
                                      type: string
                                    port:
                                      default: 26379
                                      format: int32
                                      type: integer
                                  required:
                                  - host
                                  type: object
                                type: array
                            required:
                            - host
                            type: object
                          ha:
                            description: HA override for this role's managed instance.
                              Nil inherits spec.redis.ha.
                            properties:
                              enabled:
                                default: true
                                description: Enabled toggles HA when the block is
                                  present.
                                type: boolean
                              image:
                                default: quay.io/opstree/redis:v8.2.5
                                description: Image is the redis image driven by the
                                  operator's RedisReplication.
                                type: string
                              replicas:
                                default: 3
                                description: |-
                                  Replicas is the RedisReplication cluster size: total redis nodes
                                  (1 primary + N-1 replicas). 3 gives one primary and two replicas.
                                format: int32
                                minimum: 2
                                type: integer
                              sentinelImage:
                                default: quay.io/opstree/redis-sentinel:v8.2.5
                                description: SentinelImage is the redis-sentinel image
                                  for the operator's RedisSentinel.
                                type: string
                              sentinels:
                                default: 3
                                description: Sentinels is the RedisSentinel cluster
                                  size. Use an odd number for quorum.
                                format: int32
                                minimum: 1
                                type: integer
                            type: object
                          maxMemory:
                            description: MaxMemory override for this role's managed
                              instance.
                            type: string
                          maxMemoryPolicy:
                            description: MaxMemoryPolicy override for this role's
                              managed instance.
                            type: string
                          resources:
                            description: Resources override for this role's managed
                              container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This is an alpha field and requires enabling the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Storage override for this role's managed
                              PVC.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: StorageClassName override for this role's
                              managed PVC.
                            type: string
                        type: object
                      reactions:
                        description: |-
                          RedisRole separates one Misskey Redis role. Presence separates the role;
                          External points it at an existing Redis, otherwise a dedicated instance is
                          provisioned (inheriting the default redis settings unless overridden here).
                        properties:
                          external:
                            description: |-
                              External points this role at an existing Redis. Mutually exclusive with the
                              managed override fields.
                            properties:
                              host:
                                description: |-
                                  Host of the external Redis. With Sentinels set this is ignored by the
                                  client but still required by Misskey's config schema.
                                type: string
                              masterName:
                                description: MasterName is the sentinel master group
                                  name (required with Sentinels).
                                type: string
                              passwordSecret:
                                description: PasswordSecret optionally references
                                  the Redis password.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              port:
                                default: 6379
                                description: Port of the external Redis.
                                format: int32
                                type: integer
                              sentinels:
                                description: |-
                                  Sentinels, when set, connects via Redis Sentinel (ioredis passthrough)
                                  instead of host/port.
                                items:
                                  description: RedisHostPort is a sentinel endpoint.
                                  properties:
                                    host:
                                      type: string
                                    port:
                                      default: 26379
                                      format: int32
                                      type: integer
                                  required:
                                  - host
                                  type: object
                                type: array
                            required:
                            - host
                            type: object
                          ha:
                            description: HA override for this role's managed instance.
                              Nil inherits spec.redis.ha.
                            properties:
                              enabled:
                                default: true
                                description: Enabled toggles HA when the block is
                                  present.
                                type: boolean
                              image:
                                default: quay.io/opstree/redis:v8.2.5
                                description: Image is the redis image driven by the
                                  operator's RedisReplication.
                                type: string
                              replicas:
                                default: 3
                                description: |-
                                  Replicas is the RedisReplication cluster size: total redis nodes
                                  (1 primary + N-1 replicas). 3 gives one primary and two replicas.
                                format: int32
                                minimum: 2
                                type: integer
                              sentinelImage:
                                default: quay.io/opstree/redis-sentinel:v8.2.5
                                description: SentinelImage is the redis-sentinel image
                                  for the operator's RedisSentinel.
                                type: string
                              sentinels:
                                default: 3
                                description: Sentinels is the RedisSentinel cluster
                                  size. Use an odd number for quorum.
                                format: int32
                                minimum: 1
                                type: integer
                            type: object
                          maxMemory:
                            description: MaxMemory override for this role's managed
                              instance.
                            type: string
                          maxMemoryPolicy:
                            description: MaxMemoryPolicy override for this role's
                              managed instance.
                            type: string
                          resources:
                            description: Resources override for this role's managed
                              container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This is an alpha field and requires enabling the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Storage override for this role's managed
                              PVC.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: StorageClassName override for this role's
                              managed PVC.
                            type: string
                        type: object
                      timelines:
                        description: |-
                          RedisRole separates one Misskey Redis role. Presence separates the role;
                          External points it at an existing Redis, otherwise a dedicated instance is
                          provisioned (inheriting the default redis settings unless overridden here).
                        properties:
                          external:
                            description: |-
                              External points this role at an existing Redis. Mutually exclusive with the
                              managed override fields.
                            properties:
                              host:
                                description: |-
                                  Host of the external Redis. With Sentinels set this is ignored by the
                                  client but still required by Misskey's config schema.
                                type: string
                              masterName:
                                description: MasterName is the sentinel master group
                                  name (required with Sentinels).
                                type: string
                              passwordSecret:
                                description: PasswordSecret optionally references
                                  the Redis password.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              port:
                                default: 6379
                                description: Port of the external Redis.
                                format: int32
                                type: integer
                              sentinels:
                                description: |-
                                  Sentinels, when set, connects via Redis Sentinel (ioredis passthrough)
                                  instead of host/port.
                                items:
                                  description: RedisHostPort is a sentinel endpoint.
                                  properties:
                                    host:
                                      type: string
                                    port:
                                      default: 26379
                                      format: int32
                                      type: integer
                                  required:
                                  - host
                                  type: object
                                type: array
                            required:
                            - host
                            type: object
                          ha:
                            description: HA override for this role's managed instance.
                              Nil inherits spec.redis.ha.
                            properties:
                              enabled:
                                default: true
                                description: Enabled toggles HA when the block is
                                  present.
                                type: boolean
                              image:
                                default: quay.io/opstree/redis:v8.2.5
                                description: Image is the redis image driven by the
                                  operator's RedisReplication.
                                type: string
                              replicas:
                                default: 3
                                description: |-
                                  Replicas is the RedisReplication cluster size: total redis nodes
                                  (1 primary + N-1 replicas). 3 gives one primary and two replicas.
                                format: int32
                                minimum: 2
                                type: integer
                              sentinelImage:
                                default: quay.io/opstree/redis-sentinel:v8.2.5
                                description: SentinelImage is the redis-sentinel image
                                  for the operator's RedisSentinel.
                                type: string
                              sentinels:
                                default: 3
                                description: Sentinels is the RedisSentinel cluster
                                  size. Use an odd number for quorum.
                                format: int32
                                minimum: 1
                                type: integer
                            type: object
                          maxMemory:
                            description: MaxMemory override for this role's managed
                              instance.
                            type: string
                          maxMemoryPolicy:
                            description: MaxMemoryPolicy override for this role's
                              managed instance.
                            type: string
                          resources:
                            description: Resources override for this role's managed
                              container.
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This is an alpha field and requires enabling the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          storage:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Storage override for this role's managed
                              PVC.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            description: StorageClassName override for this role's
                              managed PVC.
                            type: string
                        type: object
                    type: object
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 2Gi
                    description: Storage size of the managed Redis PVC.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: StorageClassName for the managed Redis PVC.
                    type: string
                type: object
              runtime:
                description: |-
                  Runtime adapts the operator to Misskey forks whose image contract (uid,
                  commands, paths, health endpoint) differs from upstream misskey/misskey.
                properties:
                  builtPath:
                    description: |-
                      BuiltPath is the pre-built assets directory copied to a writable emptyDir so
                      the image can write compile-config output. Default /misskey/built. Set to an
                      empty string to disable the copy for forks that do not need it.
                    type: string
                  configPath:
                    description: |-
                      ConfigPath overrides where the rendered default.yml is mounted in the
                      container. Default /misskey/.config/default.yml.
                    type: string
                  healthPath:
                    description: HealthPath overrides the app HTTP health/probe path.
                      Default /api/server-info.
                    type: string
                  migrateCommand:
                    description: |-
                      MigrateCommand overrides the migration Job command.
                      Default ["pnpm","run","migrate"].
                    items:
                      type: string
                    type: array
                  runAsUser:
                    description: RunAsUser overrides the container uid. Default 991
                      (upstream misskey image).
                    format: int64
                    type: integer
                  startCommand:
                    description: |-
                      StartCommand overrides the app/worker container command.
                      Default ["pnpm","run","start"].
                    items:
                      type: string
                    type: array
                type: object
              search:
                description: Search configures the fulltext search provider. MeiliSearch
                  by default.
                properties:
                  meilisearch:
                    description: Meilisearch configures the MeiliSearch backend (provider=meilisearch).
                    properties:
                      external:
                        description: External points Misskey at an existing MeiliSearch.
                          When set, none is managed.
                        properties:
                          apiKeySecret:
                            description: APIKeySecret references the API key for the
                              external MeiliSearch.
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          host:
                            description: Host of the external MeiliSearch.
                            type: string
                          port:
                            default: 7700
                            description: Port of the external MeiliSearch.
                            format: int32
                            type: integer
                          ssl:
                            description: SSL toggles https when talking to the external
                              MeiliSearch.
                            type: boolean
                        required:
                        - apiKeySecret
                        - host
                        type: object
                      image:
                        default: getmeili/meilisearch:v1.11
                        description: Image for the managed MeiliSearch StatefulSet.
                        type: string
                      index:
                        description: Index name used by Misskey. Defaults to the sanitized
                          host of spec.url.
                        type: string
                      masterKeySecret:
                        description: |-
                          MasterKeySecret references an existing master key. When empty the operator
                          generates one and stores it in a Secret named <misskey>-meilisearch.
                        properties:
                          key:
                            description: The key of the secret to select from.  Must
                              be a valid secret key.
                            type: string
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                          optional:
                            description: Specify whether the Secret or its key must
                              be defined
                            type: boolean
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      resources:
                        description: Resources for the managed MeiliSearch container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      scope:
                        default: local
                        description: 'Scope selects which notes are indexed: local
                          (this server) or global.'
                        enum:
                        - local
                        - global
                        type: string
                      storage:
                        anyOf:
                        - type: integer
                        - type: string
                        default: 10Gi
                        description: Storage size of the managed MeiliSearch PVC.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: StorageClassName for the managed MeiliSearch
                          PVC.
                        type: string
                    type: object
                  provider:
                    default: meilisearch
                    description: Provider selects the fulltext search backend.
                    enum:
                    - meilisearch
                    - sqlLike
                    - sqlPgroonga
                    type: string
                type: object
              setupPassword:
                description: |-
                  SetupPassword configures the initial-setup admin password written to
                  default.yml as `setupPassword`. When the block is present with no
                  secretRef, the operator generates a random password into the Secret
                  "<name>-setup" (key SETUP_PASSWORD); retrieve it to complete first setup.
                  Omit the block to leave setupPassword out of default.yml entirely.
                properties:
                  secretRef:
                    description: |-
                      SecretRef references an existing secret key holding the setup password.
                      When nil, the operator generates one into the Secret "<name>-setup".
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              tenancy:
                description: |-
                  Tenancy configures namespace-scoped isolation, assuming the namespace is
                  dedicated to this instance.
                properties:
                  dedicated:
                    description: |-
                      Dedicated declares the namespace is dedicated to this instance, which is
                      required to manage the namespace-scoped ResourceQuota/LimitRange below.
                    type: boolean
                  limitRange:
                    description: LimitRange, when set with Dedicated, creates a Container-scoped
                      LimitRange.
                    properties:
                      default:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Default container limits.
                        type: object
                      defaultRequest:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: DefaultRequest container requests.
                        type: object
                      max:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Max container limits.
                        type: object
                    type: object
                  quota:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Quota, when set with Dedicated, becomes the ResourceQuota spec.hard
                      (e.g. cpu, memory, requests.cpu, limits.memory, pods).
                    type: object
                type: object
                x-kubernetes-validations:
                - message: tenancy.quota with compute resources requires tenancy.limitRange.default
                    and defaultRequest, otherwise pods without explicit requests are
                    rejected by the quota
                  rule: '!has(self.quota) || !self.quota.exists(k, k == ''cpu'' ||
                    k == ''memory'' || k.startsWith(''requests.'') || k.startsWith(''limits.''))
                    || (has(self.limitRange) && has(self.limitRange.default) && has(self.limitRange.defaultRequest))'
              tenant:
                description: |-
                  Tenant is the tenant identifier stamped as the cloudnative-misskey.dev/tenant
                  label on every managed resource and pod (incl. CNPG pods), for per-tenant log
                  and metric routing. Immutable; defaults to the namespace when omitted. Setting
                  it after creation (from unset) also relabels existing pods on the next
                  reconcile and shifts the tenant key, so fix it at creation time.
                maxLength: 63
                pattern: ^[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?$
                type: string
                x-kubernetes-validations:
                - message: tenant is immutable
                  rule: self == oldSelf
              url:
                description: |-
                  URL is the public-facing URL of the instance, e.g. https://misskey.example.com/.
                  Do not change this after the instance has been initialized.
                pattern: ^https?://.+
                type: string
              worker:
                description: Worker configures the job queue Deployment (runs with
                  MK_ONLY_QUEUE).
                properties:
                  autoscaling:
                    description: |-
                      Autoscaling replaces the static Replicas with a HorizontalPodAutoscaler
                      (CPU/memory) or, when queues are set, a KEDA ScaledObject (BullMQ queue
                      depth). HPA needs metrics-server; queue scaling needs KEDA installed.
                    properties:
                      enabled:
                        default: true
                        description: Enabled toggles autoscaling when the block is
                          present.
                        type: boolean
                      maxReplicas:
                        description: MaxReplicas is the upper bound.
                        format: int32
                        minimum: 1
                        type: integer
                      minReplicas:
                        description: |-
                          MinReplicas is the lower bound. Default 1; use >=2 so the PodDisruptionBudget
                          (maxUnavailable=1) still allows node drains.
                        format: int32
                        minimum: 0
                        type: integer
                      queues:
                        description: |-
                          Queues, when set, switches the mechanism to a KEDA ScaledObject that scales on
                          BullMQ queue wait-list depth. Requires KEDA in the cluster. Meaningful for the
                          worker; typically deliver and inbox.
                        items:
                          description: QueueScaleTrigger scales on the wait-list depth
                            of one BullMQ queue.
                          properties:
                            listLength:
                              description: ListLength is the target average wait-list
                                length per replica.
                              format: int32
                              minimum: 1
                              type: integer
                            listName:
                              description: |-
                                ListName overrides the exact Redis key watched. Defaults to the computed
                                BullMQ wait-list key for Name.
                              type: string
                            name:
                              description: Name is the BullMQ queue name, e.g. deliver,
                                inbox.
                              type: string
                          required:
                          - listLength
                          - name
                          type: object
                        type: array
                      targetCPUUtilizationPercentage:
                        description: TargetCPUUtilizationPercentage adds a CPU utilization
                          metric. Unset omits it.
                        format: int32
                        type: integer
                      targetMemoryUtilizationPercentage:
                        description: TargetMemoryUtilizationPercentage adds a memory
                          utilization metric. Unset omits it.
                        format: int32
                        type: integer
                    required:
                    - maxReplicas
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector constrains scheduling to matching nodes.
                    type: object
                  replicas:
                    description: Replicas is the desired number of pods.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources describes compute requests/limits for the
                      container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations allow scheduling onto tainted nodes.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
            required:
            - image
            - url
            type: object
          status:
            description: MisskeyStatus defines the observed state of a Misskey instance.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the instance state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is a coarse, human-readable state summary.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
