apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Aggregates the CRD, RBAC and controller manager into one deployable bundle.
resources:
  - ../crd
  - ../rbac
  - ../manager
  - ../network-policy
